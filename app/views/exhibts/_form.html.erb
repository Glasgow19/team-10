<%= form_with(model: exhibt, local: true) do |form| %>
  <% if exhibt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exhibt.errors.count, "error") %> prohibited this exhibt from being saved:</h2>

      <ul>
        <% exhibt.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
